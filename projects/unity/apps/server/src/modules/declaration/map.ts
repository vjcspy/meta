export type MapOptions = {
  mapId: string;
  creatorId: string;
};

export type MapMetadata = {
  mapId: string;
  creatorId: string;
};

export type MapPresenceMessage = {
  type: string;
  payload: any;
};
