mutation resetPassword($mail: String!, $resetPasswordToken: String!, $newPassword: String!) {
    resetPassword(
        email: $mail, resetPasswordToken: $resetPasswordToken, newPassword: $newPassword
    )
}
