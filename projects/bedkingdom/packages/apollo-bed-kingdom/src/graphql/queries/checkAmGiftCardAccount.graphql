query checkAmGiftCardAccount($input: AmGiftCardAccountInput! ){
    amGiftCardAccount(
        input: $input
    ) {
        code
        expiration_date
        status
        current_balance{
            value
            currency
        }
    }
}