query amMostviewedGroups($id: String!){
    amMostviewedGroups(uid: $id) {
        items{
            block_title
            block_layout
            add_to_cart
            position
            items {
                ...BedKingdomProductItemMostViewed
            }
        }
    }
}
